# dynamic-json-validator
A Node.js + TypeScript API that dynamically infers JSON schemas, validates incoming data, and generates TypeScript types automatically. Perfect for developers who want to enforce data integrity without writing schemas manually.
